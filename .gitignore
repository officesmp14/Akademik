# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# env files -- JANGAN pernah commit, berisi kredensial asli
# (.env.local.example TIDAK di-ignore, itu template kosong yang aman dibagikan)
.env
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Claude Code -- settings.local.json berisi riwayat izin yang bisa memuat
# nilai sensitif (mis. API key) yang tercatat dari perintah yang pernah
# dijalankan. Jangan commit.
.claude/settings.local.json

# scratch/reference files yang tidak perlu ikut ke repo
/temp
